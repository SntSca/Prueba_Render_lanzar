import { Component } from '@angular/core';

@Component({
  selector: 'app-pagina-inicial',       // Se recomienda kebab-case para el selector
  templateUrl: './paginaInicial.html',   // Asegúrate que este archivo exista
  styleUrls: ['./paginaInicial.css']     // Debe ser style**Urls** y en array
})
export class PaginaInicialComponent { }  // Nombre de clase consistente con Angular
